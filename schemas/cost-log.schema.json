{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mumei.dev/schemas/cost-log.schema.json",
  "title": "mumei cost-log JSONL entry",
  "description": "One line of .mumei/specs/<feature>/cost-log.jsonl (spec vehicle) or .mumei/plans/<slug>/cost-log.jsonl (plan vehicle). Append-only. Per-feature, archived with the feature.",
  "additionalProperties": false,
  "type": "object",
  "required": [
    "ts",
    "feature",
    "phase"
  ],
  "properties": {
    "ts": {
      "format": "date-time",
      "description": "ISO 8601 UTC.",
      "type": "string"
    },
    "feature": {
      "type": "string"
    },
    "wave": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "iteration": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ]
    },
    "agent": {
      "description": "Reviewer / curator / SDK agent short name.",
      "type": "string"
    },
    "phase": {
      "description": "before: launch bookmark (no token usage). after: completion record with usage payload.",
      "anyOf": [
        {
          "const": "before",
          "type": "string"
        },
        {
          "const": "after",
          "type": "string"
        }
      ]
    },
    "input_tokens": {
      "minimum": 0,
      "description": "Anthropic API canonical field. Tokens AFTER the cache breakpoint (i.e., not cached).",
      "type": "integer"
    },
    "output_tokens": {
      "minimum": 0,
      "type": "integer"
    },
    "cache_read_input_tokens": {
      "minimum": 0,
      "description": "Cache HIT (tokens served from cache). Anthropic canonical field.",
      "type": "integer"
    },
    "cache_creation_input_tokens": {
      "minimum": 0,
      "description": "Cache MISS first-write (tokens that just landed in a new cache breakpoint). Anthropic canonical field.",
      "type": "integer"
    },
    "diff_hash": {
      "description": "Content hash (git tree id of the working-tree snapshot) of the review surface the reviewer ran against, written on phase=after records by hooks/subagent-cost-log.sh. Optional: omitted when git/HEAD is unavailable; older records predate the field.",
      "type": "string"
    }
  }
}
