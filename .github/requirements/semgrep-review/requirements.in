# Review-pipeline grounding scan (review-reusable.yml semgrep step).
# Frozen at 1.111.0 so AI reviewer signal stays comparable across runs;
# bumping disrupts grounding fingerprints. Dependabot is configured to
# ignore semgrep major bumps for this lock only (see dependabot.yml).
#
# Regenerate the locked requirements.txt on Python 3.14 (matches the
# `actions/setup-python` version pinned in review-reusable.yml):
#   python3.14 -m venv .tmp/pip-compile-venv
#   .tmp/pip-compile-venv/bin/pip install pip-tools
#   .tmp/pip-compile-venv/bin/pip-compile \
#     --generate-hashes --strip-extras --allow-unsafe \
#     --output-file=.github/requirements/semgrep-review/requirements.txt \
#     .github/requirements/semgrep-review/requirements.in
semgrep==1.111.0
